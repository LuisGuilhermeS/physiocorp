<?php
// Defina os parâmetros de conexão com o banco de dados
$host = 'localhost';      // Normalmente é localhost
$usuario = 'root';        // Usuário do banco de dados
$senha = '';              // Senha do banco (deixe vazio se não tiver)
$banco = 'physiocorp';    // Nome do banco de dados

// Criar conexão
//$conn = new mysqli($host, $usuario, $senha, $banco);

// Verificar se houve erro na conexão
//if ($conn->connect_error) {
  //  die("Falha na conexão: " . $conn->connect_error);
//}

// Definir o charset como UTF-8 para suportar acentuação
//$conn->set_charset("utf8");
//?>
